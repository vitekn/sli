#ifndef TRIANGLEUTIL_H
#define TRIANGLEUTIL_H

#include "PointUtil.h"
#include "Triangle.h"
#include <istream>
#include <string>

struct TriangleUtil
{
    
    
};

std::istream& operator>>(std::istream& is, Triangle& p);

#endif
